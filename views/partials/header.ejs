<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Profile Storing App — Create, read, update and delete user profiles." />
  <title><%= title %> | Profile App</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'><rect width='32' height='32' rx='6' fill='%236366f1'/><text x='16' y='22' font-size='18' text-anchor='middle' fill='white'>P</text></svg>" />
</head>
<body>
  <div class="app-wrapper">
    <nav class="navbar">
      <a href="/" class="navbar__brand">
        <span class="navbar__brand-icon">P</span>
        ProfileApp
      </a>
      <div class="navbar__links">
        <a class="navbar__link<%= typeof title !== 'undefined' && title === 'Create User' ? ' navbar__link--active' : '' %>" href="/">
          ✦ Create
        </a>
        <a class="navbar__link<%= typeof title !== 'undefined' && title === 'All Users' ? ' navbar__link--active' : '' %>" href="/read">
          ◎ Users
        </a>
      </div>
    </nav>
    <main class="content">
