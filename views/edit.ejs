<%- include('partials/header', { title: 'Edit User' }) %>

  <h1 class="text-3xl tracking-tighter mb-6">Update User</h1>

  <form action="/update/<%= user._id %>" method="post" class="flex flex-col gap-3 max-w-sm">
    <input
      type="text"
      name="name"
      placeholder="Full name"
      value="<%= user.name %>"
      required
      class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded outline-none focus:border-blue-500 transition-colors"
    />
    <input
      type="email"
      name="email"
      placeholder="Email address"
      value="<%= user.email %>"
      required
      class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded outline-none focus:border-blue-500 transition-colors"
    />
    <input
      type="url"
      name="image"
      placeholder="Profile image URL"
      value="<%= user.image %>"
      class="px-5 py-2 bg-transparent border-2 border-zinc-700 rounded outline-none focus:border-blue-500 transition-colors"
    />
    <button
      type="submit"
      class="px-5 py-2 bg-blue-600 hover:bg-blue-500 rounded-lg transition-colors font-medium cursor-pointer"
    >
      Save Changes
    </button>
  </form>

<%- include('partials/footer') %>